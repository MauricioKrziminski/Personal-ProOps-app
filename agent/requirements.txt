# Versões FIXADAS e RESOLVIDAS (verificadas com pip, 30/08/2026) — nunca faixas.
# A lição do `gemini-flash-latest` (alias que migrou sozinho e quebrou o parse em
# produção) vale para dependência: a que muda sozinha quebra sem ninguém mexer.

# --- web ---
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
orjson==3.12.0
httpx==0.28.1
python-multipart==0.0.32

# --- agente ---
langgraph==1.2.11
langgraph-checkpoint-postgres==3.1.2
langchain-core==1.6.1
langchain-google-genai==4.3.7
psycopg[binary,pool]==3.3.4
python-dateutil==2.9.0.post0
langfuse==4.15.1
# exigido pelo langfuse.langchain (o CallbackHandler recusa subir só com langchain-core)
langchain==1.3.18

# --- google / auth ---
google-cloud-tasks==2.24.0
google-auth==2.57.0
pyjwt==2.13.0
