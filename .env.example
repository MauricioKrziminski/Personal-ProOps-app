# App Expo — apenas chaves públicas (anon). Copie para .env e preencha.
# Segredos de servidor (Gemini, Groq, WhatsApp) ficam em supabase/.env.example.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
