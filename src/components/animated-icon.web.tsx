/**
 * No web não existe splash nativa para cobrir — a página já monta com o app.
 * O componente existe só para a importação de `_layout.tsx` resolver nas duas
 * plataformas.
 */
export function AnimatedSplashOverlay() {
  return null;
}
