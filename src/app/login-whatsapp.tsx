import { LoginScreen } from '@/components/login-screen';

/** Entrar pelo WhatsApp (Phone OTP) — o caminho de quem já tinha conta antes do e-mail. */
export default function LoginWhatsApp() {
  return <LoginScreen />;
}
